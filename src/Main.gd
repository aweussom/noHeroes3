extends Node
## Root coordinator: boots the game and switches between the adventure map and battle.
## Attach to the root node of Main.tscn (created in the first editor session), and set
## Main.tscn as the project's main scene.

func _ready() -> void:
	# TODO(M1): load a converted map -> MapModel -> instance MapView -> show it.
	pass
